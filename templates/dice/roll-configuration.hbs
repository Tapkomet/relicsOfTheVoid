<fieldset>
    <legend>{{ localize "ROTV.RollConfiguration.Configuration" }}</legend>
    <div class="form-group">
        <label>{{ localize "ROTV.RollMode" }}</label>
        <select name="rollMode">
            {{ selectOptions rollModesOptions selected=rollMode localize=true }}
        </select>
    </div>
</fieldset>
