<div class="tab biography" data-group="primary" data-tab="biography">

    {{#if editingDescriptionTarget}}

    {{ editor enriched.editing target=editingDescriptionTarget button=false editable=true engine="prosemirror"
              collaborate=true }}

    {{else}}

    {{!-- Ideals, Bonds, & Flaws --}}
    <div class="top flexrow">

        {{~> "rotv.biography-textbox" name="system.details.ideal" value=system.details.ideal label="ROTV.Ideals"
            icon="fas fa-seedling" ~}}

        {{~> "rotv.biography-textbox" name="system.details.bond" value=system.details.bond label="ROTV.Bonds"
            icon="fas fa-link" ~}}

        {{~> "rotv.biography-textbox" name="system.details.flaw" value=system.details.ideal label="ROTV.Flaws"
            icon="fas fa-heart-crack" ~}}

    </div>

    {{!-- Public Information --}}
    <div class="middle" data-target="system.details.biography.public">

        <h3 class="icon">
            <a class="editor-edit" data-action="editDescription"
               aria-label="{{ localize "ROTV.BiographyPublicEdit" }}">
                <i class="fas fa-feather"></i>
            </a>
            <rotv-icon src="systems/rotv/icons/svg/ink-pot.svg"></rotv-icon>
            <span class="roboto-upper">{{ localize "ROTV.Public" }}</span>
        </h3>

        <div class="editor">
            <div class="editor-content">{{{ enriched.public }}}</div>
        </div>

    </div>

    {{!-- Details --}}
    <div class="bottom" data-target="system.details.biography.value">

        <h3 class="icon">
            <a class="editor-edit" data-action="editDescription" aria-label="{{ localize "ROTV.DetailsEdit" }}">
                <i class="fas fa-feather"></i>
            </a>
            <rotv-icon src="systems/rotv/icons/svg/ink-pot.svg"></rotv-icon>
            <span class="roboto-upper">{{ localize "ROTV.Details" }}</span>
        </h3>

        <div class="editor">
            <div class="editor-content">{{{ enriched.value }}}</div>
        </div>

    </div>

    {{/if}}

</div>
