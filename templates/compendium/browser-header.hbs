<div>
    <div class="mode-toggle">
        {{#if showModeToggle}}
        <slide-toggle class="mode-slider header-control" data-action="toggleMode" {{ checked isAdvanced }}
                      id="{{ partId }}-mode-toggle"></slide-toggle>
        <label for="{{ partId }}-mode-toggle">{{ localize "ROTV.Advanced" }}</label>
        {{/if}}
    </div>
    <h2>{{ localize "ROTV.CompendiumBrowser.Title" }}</h2>
</div>
