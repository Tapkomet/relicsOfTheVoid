<div class="flexrow">
    {{~#if displaySelection}}
    <div class="count {{ invalid }}">
        {{{ localize "ROTV.CompendiumBrowser.Selection.Label" summary=summary }}}
    </div>
    <button type="submit">
        {{ localize "ROTV.CompendiumBrowser.Selection.Select" }}
    </button>
    {{/if~}}
</div>
