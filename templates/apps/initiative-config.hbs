<form autocomplete="off">
    <p class="notes">{{localize "ROTV.InitiativeConfigHint"}}</p>

    <div class="form-group">
        <label>{{localize "ROTV.AbilityModifier"}}</label>
        <div class="form-fields">
            <select name="system.attributes.init.ability">
                {{selectOptions abilities selected=ability blank="" labelAttr="label"}}
            </select>
        </div>
    </div>

    <div class="form-group">
        <label>{{localize "ROTV.InitiativeBonus"}}</label>
        <div class="form-fields">
            <input name="system.attributes.init.bonus" type="text" value="{{bonus}}">
        </div>
    </div>

    <div class="form-group">
        <label>{{localize "ROTV.FlagsAlert"}}</label>
        <input type="checkbox" name="flags.rotv.initiativeAlert" {{checked initiativeAlert}}>
    </div>

    <div class="form-group">
        <label>{{localize "ROTV.FlagsInitiativeAdv"}}</label>
        <input type="checkbox" name="flags.rotv.initiativeAdv" {{checked initiativeAdv}}>
    </div>

    <button type="submit"><i class="far fa-save"></i> {{ localize "Submit"}}</button>
</form>
