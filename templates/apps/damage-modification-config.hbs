<form autocomplete="off">
    <h3>{{ localize "ROTV.DamageTypes" }}</h3>
    <p class="note">{{ localize "ROTV.DamageModification.Hint" }}</p>
    <ol class="trait-list modifications">
        {{#each modifications}}
        <li>
            <label>
                {{ label }}
                <input type="text" name="system.traits.dm.amount.{{ @key }}" value="{{ value }}"
            </label>
        </li>
        {{/each}}
    </ol>

    <h3>{{ localize "ROTV.DamagePhysicalBypass" }}</h3>
    <p class="note">{{ localize "ROTV.DamageModification.BypassHint" }}</p>
    {{> "rotv.trait-list" choices=bypasses prefix="system.traits.dm.bypasses" }}

    <button type="submit" name="submit" value="1">
        <i class="fa-regular fa-save"></i> {{ localize "ROTV.TraitSave" }}
    </button>
</form>
