<form>
    {{#if isCharacter}}

    <div class="hp-field form-group">
        <input type="number" class="number-lg" name="hp.max" value="{{source.max}}"
               placeholder="{{hp.max}}" data-tooltip="ROTV.HitPointsOverride">
    </div>

    <div class="form-group">
        <label>{{localize "ROTV.HitPointsBonusLevel"}}</label>
        <input type="text" name="hp.bonuses.level" value="{{hp.bonuses.level}}">
    </div>
    <div class="form-group">
        <label>{{localize "ROTV.HitPointsBonusOverall"}}</label>
        <input type="text" name="hp.bonuses.overall" value="{{hp.bonuses.overall}}">
    </div>

    {{else}}

    <div class="hp-field form-group">
        <input type="number" class="number-lg" name="hp.max" value="{{ source.max }}" data-tooltip="ROTV.HitPointsMax">
    </div>

    <div class="form-group">
        <label>{{localize "ROTV.HPFormula"}}</label>
        <div class="form-fields">
            <input type="text" name="hp.formula" value="{{hp.formula}}">
            <button class="roll-hit-points"><i class="fas fa-dice"></i> {{localize "ROTV.Roll"}}</button>
        </div>
    </div>

    {{/if}}

    <hr>

    <div class="form-group">
        <label>{{localize "ROTV.HitPointsTempMaxShort"}}</label>
        <div class="form-fields">
            <input type="number" name="hp.tempmax" value="{{source.tempmax}}">
        </div>
    </div>

    <button type="submit">
        <i class="far fa-save"></i> {{localize "Save"}}
    </button>
</form>
